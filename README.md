# AXE BUILD v1.1.32 · AXE HUB

외부 공개용 추천세팅 / 개조서 / 제보 서비스입니다.

## V1.0.2 UI / 내부 추천세팅 UX 반영

- 메인 우측의 의미가 불분명한 장식 카드를 제거하고 **실제 공식 추천세팅 미리보기**로 교체
- 메인에서 **추천세팅 바로 보기 / 공식 세팅 보기 / 빠른 탐색** 제공
- 추천세팅 목록을 텍스트 카드에서 **겉옷·상의·하의·신발 슬롯형 카드**로 변경
- 개조서를 **목록 + 상세 도감형 UI**로 변경하고 부위 필터 추가
- 제보 화면에 **누락 → 수정 → 검수 반영** 흐름을 시각적으로 정리
- Discord 로그인 안내에 **서버 가입/메시지 접근 권한을 요청하지 않음**을 명시
- 기존 Supabase / Discord OAuth / 작성 / 복제 / 즐겨찾기 / 제보 / 관리자 기능 유지

## 적용

이 ZIP의 파일 전체를 기존 GitHub 저장소 루트에 덮어쓰기한 뒤 Commit 하면 됩니다.
GitHub와 Vercel이 연결되어 있으면 자동 배포됩니다.

`.env.local`은 GitHub에 올리지 않습니다.

Vercel 환경변수는 기존 값을 그대로 유지합니다.

```env
VITE_SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=sb_publishable_YOUR_KEY
VITE_SITE_URL=https://YOUR-SITE.vercel.app
VITE_AXE_CONTACT_URL=
```

**DB SQL 변경 없음.** 기존 AXE HUB Supabase를 그대로 사용합니다.


## V1.0.3

실제 AXE 회사 브랜딩과 팀복 이미지를 사이트 전반에 반영했습니다.
DB 스키마 변경 없이 기존 AXE HUB Supabase를 그대로 사용합니다.


## V1.0.4

장비 상세 가독성, 실제 접두/접미 옵션 표시, 작성글 수정/삭제,
그리고 하단 AXE 모집 배너의 컴팩트화가 핵심입니다.


## V1.1.0

추천/비추천, 댓글, 내 프리셋, 공지사항, 승인형 닉네임과 우측 고정 AXE 리모컨 메뉴를 추가했습니다.
이 버전은 `SUPABASE-MIGRATION-V1.1.0.sql` 실행이 필수입니다.


## V1.1.1

추천세팅 상세를 AXE NET 내부용 최종 UI 구조에 맞춰
`2x2 장비 + 고정 옵션 툴팁 + 전체 옵션 요약` 방식으로 변경했습니다.

기존 V1.1.0 Supabase 마이그레이션 이후 추가 SQL은 없습니다.


## V1.1.2

업로드된 실제 AXE NET 프로젝트의 `src/modules/info/infoView.js`와
`src/modules/info/info.css`의 추천세팅 구현을 기준으로 외부용 상세 UI를 재구성했습니다.

특히 AXE NET v1.46.4에서 해결된 `tooltip pointer-events:none` hover 안정화
방식까지 그대로 반영했습니다.


## V1.1.3

고정형 카테고리와 생활 2차 카테고리, 공지 직접 작성,
닉네임 승인 바로가기, 컨텐츠 옆 도킹 리모컨을 반영했습니다.


## V1.1.4

빠른 리모컨을 왼쪽 사이드로 옮기고,
기존 오른쪽 리모컨 자리에는 AXE 모집 홍보물을 고정 배치했습니다.

닉네임 승인 기능이 보이지 않는 경우 관리자 여부를 바로 확인할 수 있도록
프로필 역할 표시와 공지 페이지 안내를 추가했습니다.


## V1.1.5

왼쪽 빠른 리모컨을 본문에서 조금 더 분리하고,
오른쪽 빈 공간은 페이지별 정보를 보여주는 250px 컨텍스트 패널로 바꿨습니다.


## V1.1.6

왼쪽 리모컨을 제거하고, 우측 패널에 빠른 메뉴와 모집 안내를 통합했습니다.
또한 추천세팅 상세 화면의 장비 보드에 실제 인게임 배경을 반영했습니다.


## V1.1.7

모집 포스터를 한 화면에서 전체 확인할 수 있도록 축소/정돈했고,
사용자가 제공한 실제 인게임 슬롯판을 단일 배경으로 사용하도록 변경했습니다.
기존 Ghost Slot 레이어는 제거했습니다.


## V1.1.8

실제 인게임 슬롯판을 억지로 슬롯 위치에 맞추는 대신,
AXE HUB 전용 2x2 장비판 + Hover 인스펙터 구조로 재설계했습니다.
모집 팝업은 홍보 포스터와 우측 상단 닫기 버튼만 남겼습니다.


## V1.1.9

슬롯 영역의 옵션 패널을 기본 숨김으로 변경했습니다.
데스크톱에서는 장비 Hover 시에만 옵션이 나타나며, 패널이 장비 Hover를
가로막지 않도록 별도의 안전한 표시 영역과 `pointer-events:none`을 사용합니다.


## V1.1.10

슬롯 UI 미세 조정:
- 장비 4칸을 더 중앙으로 정렬
- 좌우 빈 공간 축소
- 슬롯판 뒤에 깔리던 배경 이미지 제거
- 대기 상태의 `GEAR OPTION` 라벨 제거
- Hover 옵션 팝업 구조는 유지


## V1.1.11

- 공지사항 카드의 제목/본문/줄간격을 키워 공지 가독성을 개선했습니다.
- 빌드 상세 상단 설명 영역을 더 밝고 크게 조정해 설명 텍스트가 한눈에 들어오도록 정리했습니다.
- 관련 입력창과 보조 텍스트 크기도 함께 보정했습니다.


## V1.1.12

- 추천세팅 상세의 `인벤토리` 문구를 제거하고 `장비 1 / AXE BUILD` 구조로 단순화했습니다.
- 세팅 작성 시 개조서 드롭다운에 개조서 이름뿐 아니라 옵션까지 함께 표시합니다.
- 장비 카드의 `%` 배지는 이제 **접두+접미의 긍정적인 % 옵션을 같은 옵션끼리 합산한 뒤, 합계가 가장 높은 대표 옵션**을 표시합니다.
  예: `이속 +14%`, `체력 +10%`.
- 메인 공지 미리보기는 52자까지만 노출합니다.
- 공지 / 추천세팅 / 내 프리셋 / 개조서 / 제보 / 관리에 실제 URL 경로를 부여했습니다.
- 추천세팅 상위 카테고리도 `/builds?category=체력` 형태로 주소에 반영합니다.
- 브라우저 뒤로가기/앞으로가기로 이전에 보던 AXE BUILD 페이지를 정상 이동할 수 있습니다.


## V1.1.13

- 상단 메뉴를 `홈 / 공지 / 추천세팅 / 내 프리셋 / 개조서 / 제보` 구조로 정리했습니다.
- `/`는 독립된 HOME, `/builds`는 추천세팅 페이지로 분리했습니다.
- 세팅 작성/수정의 태그 입력을 직접 타이핑 방식에서 다중선택 드롭다운으로 변경했습니다.
- `체력 + 이동속도`처럼 여러 태그를 동시에 선택할 수 있고 선택된 태그는 칩으로 확인/삭제할 수 있습니다.
- `인기`는 작성 태그가 아니라 추천·조회 반응으로 자동 분류됩니다.
- 데스크톱의 `세팅 작성` 버튼은 추천세팅 페이지 상단 1곳만 남겼습니다.
- 모바일의 `+` 버튼도 추천세팅 페이지에서만 표시합니다.


## V1.1.14

- 헤더에서 AXE 로고 옆 `AXE BUILD`를 `BUILD`로 정리했습니다.
  로고 자체에 AXE가 들어 있으므로 화면에서는 자연스럽게 `AXE BUILD`로 읽힙니다.
- 보조 문구도 `AXE HUB · PUBLIC BUILD LAB`에서 `PUBLIC BUILD LAB`로 단순화했습니다.
- 하단의 중복 AXE BUILD 브랜드 영역과 `AXE HUB · External Public Service` 문구를 포함한 footer를 완전히 제거했습니다.
- 페이지 하단의 불필요하게 큰 빈 영역도 함께 사라집니다.


## V1.1.15

- 추천세팅 카드와 상세 화면에서 **작성자 닉네임을 별도 BUILDER 영역으로 강조**했습니다.
- 승인된 회사명이 있으면 작성자 닉네임 옆에 회사 태그가 함께 표시됩니다.
- 프로필 설정에서 닉네임과 회사명을 각각 신청할 수 있습니다.
- 회사명은 닉네임과 동일하게 관리자 승인 후 적용됩니다.
- 관리 센터에 `회사명 신청` 탭과 대기 건수를 추가했습니다.
- 승인된 회사명은 기존 작성 빌드와 댓글에도 즉시 반영됩니다.
- 공지 페이지 우측 패널은 최신 공지 전문 대신 공지 채널 설명만 보여줍니다.

**V1.1.15는 새 SQL 마이그레이션이 필요합니다.**
배포 전 `SUPABASE-MIGRATION-V1.1.15.sql`을 AXE HUB Supabase SQL Editor에서 1회 실행하세요.


## V1.1.16

공개 배포 전 개인정보/업로드 안전장치를 정리했습니다.

- Discord OAuth 추가 scope를 `identify`만 요청하도록 변경했습니다.
- 첫 로그인 전에 AXE BUILD가 요청하는 정보와 요청하지 않는 정보가 보이는 로그인 안내를 추가했습니다.
- Discord 비밀번호, 서버 채팅, DM, 서버 목록에는 접근하지 않는다는 점을 명시했습니다.
- 제보 증빙 이미지는 PNG/JPG/WEBP만 허용하고 최대 5MB로 제한했습니다.
- 저장 파일 확장자는 원본 파일명이 아니라 브라우저가 확인한 MIME 타입을 기준으로 생성합니다.
- 업로드 시 `contentType`을 명시합니다.
- Vercel에 `nosniff`, `DENY frame`, Referrer-Policy, Permissions-Policy 등 기본 보안 헤더를 추가했습니다.

**V1.1.16 자체의 새 SQL은 없습니다.**
단, V1.1.15 회사명 기능을 사용하려면 기존 `SUPABASE-MIGRATION-V1.1.15.sql`이 적용되어 있어야 합니다.


## V1.1.17

추천세팅 카테고리 분류 기준을 명확하게 정리했습니다.

- `무법지대 / 체력 / 이동속도 / 생활` 카테고리는 **작성자가 직접 선택한 태그만** 기준으로 분류합니다.
- 예를 들어 `이동속도` 태그만 선택한 빌드에 체력 개조서가 포함돼 있어도 체력 카테고리에는 노출되지 않습니다.
- `체력 + 이동속도` 빌드라면 작성자가 두 태그를 모두 선택해야 두 카테고리에 노출됩니다.
- 생활 세부 카테고리 역시 `벌목 / 낚시 / 채광 / 택배` 태그를 직접 선택한 경우에만 해당 세부 분류에 노출됩니다.
- 선택한 개조서는 카테고리를 자동 변경하지 않고, 작성 화면에서 **추천 태그**만 제안합니다.
- 추천 태그는 `+ 체력`, `+ 이동속도`처럼 버튼을 눌러 작성자가 직접 추가해야 적용됩니다.
- 빌드 검색에서는 개조서 이름/분류/옵션까지 계속 검색할 수 있습니다.

SQL 변경 없음.


## V1.1.18

외부 공개 운영을 위한 서버단 도배 방지 장치를 추가했습니다.

- 빌드 작성: **10분 3개 / 24시간 15개**
- 댓글: **1분 5개 / 24시간 80개**
- 제보: **10분 3개 / 24시간 12개**
- 관리자 계정은 제한에서 제외됩니다.
- 제한은 브라우저 버튼이 아니라 **Supabase DB Trigger에서 Discord 로그인 사용자 UUID 기준**으로 검사합니다.
- 병렬 요청으로 제한을 우회하기 어렵도록 사용자/행동별 DB advisory lock을 사용합니다.
- 제한에 걸리면 `약 N분 후 다시 시도해주세요`처럼 남은 대기시간을 알려줍니다.
- 같은 빌드 내용은 24시간 내 중복 게시를 차단합니다.
- 같은 빌드에 같은 댓글을 연속 등록하는 것을 차단합니다.
- 동일한 제보 내용은 24시간 내 중복 등록을 차단합니다.
- 제보 이미지가 먼저 업로드된 뒤 DB 등록이 차단된 경우 프론트에서 해당 업로드 파일 삭제를 시도합니다.

**V1.1.18은 `SUPABASE-MIGRATION-V1.1.18.sql` 1회 실행이 필수입니다.**


## V1.1.19

메인 히어로 영역에 프로필 진입 동선을 추가했습니다.

- 로그인 상태: `추천세팅 바로 보기` 옆에 **선형 톱니바퀴 아이콘 + 프로필 설정** 보조 버튼 표시
- 비로그인 상태: 같은 위치에 **Discord 로그인** 보조 버튼 표시
- 프로필 버튼은 골드 메인 CTA보다 한 단계 약한 다크 아웃라인 스타일로 처리해 화면이 난잡해지지 않도록 했습니다.
- `프로필 설정` 클릭 시 기존 닉네임/회사명 신청 프로필 모달을 그대로 사용합니다.
- 이모지 톱니바퀴가 아니라 UI에 맞는 단색 SVG 라인 아이콘을 사용합니다.

SQL 변경 없음. V1.1.18 도배 방지 SQL은 기존대로 적용되어 있어야 합니다.


## V1.1.20

메인 히어로 소개 문구를 서비스 성격이 더 자연스럽게 전달되도록 조정했습니다.

- `이용자들이 직접 공유한 다양한 세팅`이라는 표현을 메인 소개 문구에 자연스럽게 포함했습니다.
- `추천세팅` 명칭은 그대로 유지합니다.
- 별도의 해명문/경고문은 추가하지 않았습니다.
- V1.1.19까지의 프로필 버튼, 보안 패치, 도배 방지 기능을 그대로 포함합니다.

SQL 변경 없음.


## V1.1.21

생활 카테고리의 2차 분류에 `요리`를 추가했습니다.

- 생활 > 전체 / 벌목 / 낚시 / 채광 / 택배 / 요리
- 빌드 작성 태그 목록에도 `요리` 추가
- 기존 분류 원칙 유지:
  - `요리` 태그를 작성자가 직접 선택한 빌드만 생활 > 요리에 노출
  - 요리 관련 개조서가 포함됐다고 자동 분류되지 않음
  - 선택한 개조서에서 `요리`가 감지되면 추천 태그로만 제안
- 생활 전체에는 기존처럼 생활 또는 생활 하위 태그가 있는 빌드가 노출됩니다.

SQL 변경 없음.


## V1.1.22

메인 화면에서 AXE BUILD가 이용자 참여형 빌드 공유 시스템이라는 점이 더 분명하게 보이도록 개선했습니다.

- 메인 설명에 `세팅 공유` 기능을 명시했습니다.
- 로그인 후 메인 CTA:
  - 추천세팅 바로 보기
  - 세팅 공유하기
  - 프로필 설정
- `세팅 공유하기`를 누르면 기존 추천세팅 작성창이 바로 열립니다.
- 비로그인 상태에서는 기존처럼 `추천세팅 바로 보기 / Discord 로그인`을 유지합니다.
- 버튼 위계는 `추천세팅 바로 보기 > 세팅 공유하기 > 프로필 설정` 순으로 구성했습니다.
- 모바일에서는 버튼이 자연스럽게 재배치됩니다.
- V1.1.21 이하의 보안 패치, 도배 방지, 요리 카테고리 등 누적 기능을 그대로 포함합니다.

SQL 변경 없음.


## V1.1.23

메인 히어로 설명 문구의 한글 줄바꿈을 개선했습니다.

- `로그인`이 `로 / 그인`처럼 글자 중간에서 끊기는 현상을 방지합니다.
- 한글 문장은 단어 단위로 다음 줄로 넘어가도록 `word-break: keep-all`을 적용했습니다.
- 긴 문자열이 생겼을 때 레이아웃이 넘치지 않도록 `overflow-wrap: break-word`도 함께 적용했습니다.
- 문구 내용과 기존 기능은 변경하지 않았습니다.

SQL 변경 없음.


## V1.1.24

메인 참여 동선과 CTA 디자인을 개선했습니다.

- `세팅 공유하기` 버튼을 로그인 여부와 관계없이 항상 노출합니다.
- 비로그인 사용자가 `세팅 공유하기`를 누르면:
  - 누구나 세팅을 공유할 수 있다는 안내
  - 작성자 구분/도배 방지를 위해 Discord 로그인이 필요하다는 설명
  - Discord 로그인 버튼
  을 표시합니다.
- 로그인 사용자는 기존처럼 `세팅 공유하기` 클릭 시 작성창이 바로 열립니다.
- 비로그인 메인 버튼 구성:
  - 추천세팅 바로 보기
  - 세팅 공유하기
  - Discord 로그인
- 로그인 메인 버튼 구성:
  - 추천세팅 바로 보기
  - 세팅 공유하기
  - 프로필 설정
- `추천세팅 바로 보기` CTA를 단순 단색 버튼에서 깊이감 있는 AXE 골드 그라디언트, 하이라이트, 화살표 아이콘을 사용한 프리미엄 스타일로 개선했습니다.
- V1.1.23 이하 보안 패치, 도배 방지, 카테고리/프로필 기능을 그대로 포함합니다.

SQL 변경 없음.


## V1.1.25

비로그인 상태의 세팅 공유 안내 문구를 공용 서비스에 맞는 존댓말/중립 표현으로 수정했습니다.

- 기존: `로그인하면 니 세팅도 직접 공유할 수 있습니다.`
- 변경: `로그인 후 직접 세팅을 등록하고 공유할 수 있습니다.`

기능 변경 없음.
SQL 변경 없음.


## V1.1.26

세팅 등록/수정 화면의 개조서 검색 기능을 수정했습니다.

- 기존 검색은 단순 문자열 `includes` 방식이라 띄어쓰기나 기호가 다르면 검색이 실패할 수 있었습니다.
- 검색 시 공백/괄호/일반 기호를 제거한 뒤 비교하도록 변경했습니다.
  - 예: `이동속도` → `이동 속도` 옵션도 검색
  - 예: `1%6%`처럼 기호 차이가 있어도 관련 문자열 검색 가능
- 개조서명뿐 아니라 분류, 적용 부위, 옵션 1~3, 비고, 표시용 드롭다운 문구까지 검색 대상으로 포함했습니다.
- 검색 결과 개수를 바로 표시합니다.
- 검색어 초기화(X) 버튼과 검색 결과 없음 안내를 추가했습니다.
- 분류 드롭다운 필터와 검색어는 함께 적용됩니다.

SQL 변경 없음.


## V1.1.27

제보 작성 시 반복 입력을 줄이기 위해 드롭다운 기반 빠른 입력을 추가했습니다.

- `분류`:
  - 현재 개조서 DB에 있는 분류를 드롭다운으로 선택
  - 새로운 분류는 `직접 입력` 선택 후 입력 가능
- `적용 부위`:
  - 현재 DB에 있는 부위 조합을 드롭다운으로 선택
  - 예: `겉옷/단독상의`
  - 새로운 부위는 `직접 입력` 가능
- `옵션 빠른 선택`:
  - 기존 개조서에 등록된 옵션을 드롭다운으로 선택하면 옵션 입력란에 자동 추가
  - 분류를 먼저 고르면 해당 분류의 옵션만 보여 목록을 줄여줌
  - 선택 후 수치/문구가 다르면 아래 textarea에서 직접 수정 가능
- 기존 자유 입력 방식은 완전히 제거하지 않고 fallback으로 유지했습니다.
- 정보 수정 제보에서 기존 개조서를 선택하면 기존처럼 정보가 자동 채워집니다.

DB 저장 형식은 기존과 동일하므로 SQL 변경 없음.


## V1.1.28

기존 개조서 DB를 활용하는 `무기` 페이지를 추가했습니다.

- 상단 메뉴: 홈 / 공지 / 추천세팅 / 내 프리셋 / 개조서 / **무기** / 제보
- 새 라우트: `/weapons`
- 현재 개조서의 분류·이름·적용부위를 기준으로 무기군을 자동 감지합니다.
- 지원 무기군:
  - 라이플
  - SMG
  - 권총
  - 샷건
  - 저격
  - 기관총
- 실제 데이터가 존재하는 무기군만 카드/필터에 표시됩니다.
- 각 무기군 카드에서:
  - 전체 연결 개조서 수
  - 접두/접미 개수
  를 확인할 수 있습니다.
- 무기 페이지에서 개조서명/옵션 검색을 지원합니다.
- 개조서를 선택하면 기존 개조서 상세 UI를 재사용해 적용 부위·옵션·성공률을 확인할 수 있습니다.
- 개조서 DB에 새 라이플/SMG 계열 개조서가 추가되면 별도 작업 없이 무기 페이지에 자동 반영됩니다.
- 현재 단계에서는 확인되지 않은 실제 무기명/가격/탄종 등의 데이터를 임의로 만들지 않았습니다.
  서버 무기별 상세 데이터가 확보되면 이후 확장할 수 있는 구조입니다.

SQL 변경 없음.


## V1.1.29

추천세팅 / 내 프리셋에 무기 슬롯을 통합했습니다.

- 세팅 작성/수정 상단에 **선택형 무기 슬롯** 추가
- 무기군:
  - 라이플
  - SMG
  - 권총
  - 샷건
  - 저격
  - 기관총
- 무기군을 먼저 선택하면 해당 무기군에 적용 가능한 접두/접미 개조서만 표시됩니다.
- 무기 슬롯은 선택 사항이라 기존 장비 4부위 세팅만 그대로 작성할 수도 있습니다.
- 추천세팅 카드에 선택된 무기군/접두/접미를 별도 스트립으로 표시합니다.
- 추천세팅 상세 화면에서 무기 슬롯을 장비 4부위보다 위에 별도 영역으로 표시합니다.
- 내 프리셋은 기존 BuildCard/BuildDetail 구조를 그대로 사용하므로 무기 정보도 함께 저장·표시됩니다.
- 전체 옵션 최대값 요약에는 선택한 무기 개조서 옵션도 함께 합산됩니다.
- 세팅 검색에서 `라이플`, `SMG` 같은 선택 무기군도 검색됩니다.
- 기존 세팅은 자동 호환되며 무기 미선택 상태로 그대로 표시됩니다.

### 필수 DB 변경
`SUPABASE-MIGRATION-V1.1.29.sql`을 배포 전에 1회 실행해야 합니다.

- `build_slots.weapon_family` 컬럼 추가
- 기존 RLS 정책은 그대로 사용
- 기존 데이터 삭제/초기화 없음


## V1.1.30

V1.1.29 무기 슬롯 추가 후 일부 브라우저에서 검은 화면이 뜨는 호환성 오류를 수정했습니다.

원인:
- V1.1.29 이전에 브라우저 localStorage에 저장된 세팅 작성 임시저장(draft)은
  `outer/top/bottom/shoes` 4개 슬롯만 가지고 있었습니다.
- V1.1.29 화면이 새 `weapon` 슬롯이 항상 존재한다고 가정하면서
  `slots.weapon.weapon_family`를 읽어 구버전 draft에서 런타임 오류가 발생했습니다.

수정:
- 구버전 4슬롯 draft를 열 때 자동으로 `weapon` 빈 슬롯을 추가합니다.
- Supabase의 기존 4슬롯 빌드도 동일하게 5슬롯 구조로 안전하게 보정합니다.
- 무기 슬롯 접근에 optional guard를 추가했습니다.
- 저장 시에도 슬롯 구조를 한 번 더 정규화해 누락 슬롯 때문에 저장이 실패하지 않도록 했습니다.
- 기존 draft를 사용자가 직접 삭제할 필요가 없습니다.

신규 SQL 없음.
단, V1.1.29의 `SUPABASE-MIGRATION-V1.1.29.sql`은 무기 슬롯 저장 기능을 위해 기존대로 적용되어 있어야 합니다.


## V1.1.31

무기 슬롯 저장 시 발생한 Supabase CHECK constraint 오류를 수정합니다.

증상:
`new row for relation "build_slots" violates check constraint "build_slots_slot_key_check"`

원인:
- 기존 `build_slots.slot_key` CHECK constraint가
  `outer / top / bottom / shoes` 네 슬롯만 허용하고 있었습니다.
- V1.1.29에서 `weapon` 슬롯을 추가했지만 기존 CHECK constraint 확장이 누락됐습니다.

수정:
- 기존 `build_slots_slot_key_check`를 제거
- `outer / top / bottom / shoes / weapon`을 허용하도록 다시 생성

### 필수
`SUPABASE-MIGRATION-V1.1.31.sql`을 Supabase SQL Editor에서 1회 실행하세요.

V1.1.31 프론트는 V1.1.30과 동일하며 신규 기능 변경은 없습니다.
V1.1.29의 `weapon_family` 컬럼 migration은 기존대로 적용되어 있어야 합니다.


## V1.1.32

무기 슬롯의 시각 표현을 장비 슬롯과 같은 언어로 통일했습니다.

- 기존의 텍스트 중심 무기 정보 블록을 제거했습니다.
- 실제 개별 무기 이미지가 아직 없는 상태에서도 어색하지 않도록
  라이플 / SMG / 권총 / 샷건 / 저격 / 기관총별 전용 실루엣 비주얼을 추가했습니다.
- 추천세팅 카드:
  - 무기를 텍스트 스트립이 아니라 장비와 동일한 `미니 슬롯` 형태로 표시
  - 무기 실루엣 + 무기군 + 접두/접미 표시
- 추천세팅 상세:
  - 무기 슬롯을 장비 인벤토리 안에 정식 슬롯으로 통합
  - 무기 슬롯은 2열 전체를 사용하는 가로형 슬롯
  - 마우스를 올리면 장비와 동일한 옵션 인스펙터에서 접두/접미 상세 확인
  - 모바일에서는 클릭 후 기존 상세 패널로 확인
- 세팅 작성/수정:
  - 무기군 선택 영역에 실제 슬롯 형태의 실시간 미리보기 추가
  - 무기군을 변경하면 해당 실루엣이 즉시 변경
- 실제 무기 이미지/개별 무기 DB가 확보되면 이후 실루엣 대신 실제 이미지를 연결할 수 있는 구조입니다.

SQL 변경 없음.
V1.1.31의 slot_key CHECK 수정 및 V1.1.29 weapon_family 컬럼은 기존대로 적용되어 있어야 합니다.
